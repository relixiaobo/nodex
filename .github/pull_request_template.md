## Summary

<!-- 1-3 句话描述这个 PR 做了什么，以及为什么 -->

Fixes #<!-- issue number -->

## Changes

<!-- 列出主要改动，按文件或功能分组 -->

-

## Checklist

- [ ] `npm run typecheck` 通过
- [ ] `npm run test:run` 通过（或说明为何跳过）
- [ ] `npm run build` 通过
- [ ] 涉及的 `docs/features/*.md` 已更新（无行为变更则勾选并注明）
- [ ] `docs/TESTING.md` 已同步（无测试变更则勾选并注明）
- [ ] 设计系统合规 — 对照 `docs/design-system.md`（无 UI 变更则勾选并注明）
- [ ] 热点文件声明（如修改了 `node-store.ts` / `OutlinerItem.tsx` / `system-nodes.ts`，在此列出）
- [ ] 新增 `SYS_*` / `NDX_*` 常量已在此声明（无则勾选）

## Test Plan

<!-- 描述如何验证这个改动。包括自动化测试和手动验证步骤 -->

## Screenshots

<!-- 如有 UI 变更，附上前后对比截图 -->
