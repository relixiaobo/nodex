/**
 * Supabase Realtime subscription — Phase 1: no-op (Loro local-only mode).
 * Phase 2 will replace this with Loro CRDT sync.
 */
export function useRealtimeNodes(_workspaceId: string | null) {
  // Phase 1: no-op — data is managed entirely by LoroDoc
}
