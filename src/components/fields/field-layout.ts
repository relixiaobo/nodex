/** Shared left inset for field value rows (bullet + value content alignment). */
export const FIELD_VALUE_INSET = 25;

/** Shared overlay z-index for dropdown/popover layers in outliner rows. */
export const FIELD_OVERLAY_Z_INDEX = 1200;
